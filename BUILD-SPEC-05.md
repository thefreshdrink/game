# BUILD-SPEC-05 — воздух экранов, читаемость карты, объём мини-игры

Пятое задание. Правила проекта — `CLAUDE.md`. Делается **после**
`BUILD-SPEC-04.md` (механика ходьбы), не параллельно: там меняется физика,
здесь — кадр, и смешивать их в одном проходе нельзя.

Источник — правки пользователя от 2026-08-30 и разбор по линзам №4
(`docs/reviews/lens-review-04-risk-and-edge.md`), где названо главное:
читаемость идёт раньше поблажек, а у экранов сейчас нет глубины.

**Что считается готовым:** карта нигде не сливается с фоном; на экранах 2
и 3 видно, что делать руками, без чтения; в кадре мини-игры есть верх, низ и
ощущение высоты.

**Порядок:** 1–3 (экраны) независимы друг от друга, делать по одной. 4–6
(мини-игра) — одна сцена, но 6 трогает вообще всё и делается **последней и
отдельным коммитом**.

**Открытые решения** помечены `РЕШИТЬ` — не додумывать, спросить.

---

## 1. Экран 1 — градиент пропасти на фоне

**Файлы:** `src/screens/question.js`, `src/minigames/fool/abyss.js`.

Экран 1 сейчас — фигура на пустом `#111111`. Тот же дизер-градиент, что
живёт под дорогой в мини-игре, даёт кадру низ и связывает вход с картой.

**Что сделать.**

- Переиспользовать градиент из `abyss.js`, не писать второй. Чистая функция
  отрисовки вынесена в `core/voidGradient.js`; `abyss.js` — тонкая обёртка
  над ней (правка 2026-09-10).
- Градиент — **позади** фигуры оракула, поверх фона.
- Появление начинается **только после того, как оракул проявился целиком**
  (текущая пиксельная сборка от лица наружу доходит до конца).
- Сам градиент — **пиксельный дизер**, как в мини-игре (правка в чате
  2026-09-10: пользователь просила именно пиксельный). Но ПОЯВЛЕНИЕ его —
  не пиксельной сборкой, а плавным фейдом по прозрачности: 2.5–3 с,
  ease-out, «глаза привыкли к темноте», а не ещё один эффект сборки.
- Дизер не должен спорить с фигурой: держать его ниже и слабее, чем в
  мини-игре (`topFrac` пониже, яркость капнута — `maxLevel`).

**Приёмка.** Первые секунды экрана 1 выглядят как сейчас. Градиент
проступает после фигуры, настолько медленно, что момент его появления
нельзя назвать точно. Текст и пункты меню читаются не хуже прежнего.

---

## 2. Экран 2 — карта как объект, и понятный жест

**Файл:** `src/screens/deck.js`, `src/core/cardRender.js`.

Две проблемы: рубашка сливается с фоном, и непонятно, что по колоде можно
вести пальцем.

### 2a. Тело карты

Сейчас под рубашку подкладывается `#111111` — тот же цвет, что воздух
сцены, поэтому карта и фон читаются как одно.

**Сделать:** подложка карты — **`#000000`**, как у плит дороги. По
дизайн-системе тело — «дыра в воздухе», и карта тело, а не воздух. Белая
штриховка рубашки на чёрном отделяется сама, без новых цветов.

`РЕШИТЬ, если не хватит:` пользователь предлагала крупную тёмно-серую
шашечку. Держим как запасной вариант — но пробуем **после** сплошного
чёрного и только если разделения всё ещё мало: на двадцати перекрытых
картах текстура рискует превратиться в рябь. Шашечка, если дойдёт до неё, —
очень крупная (не мельче 16 экранных px) и в два тона внутри чёрного, без
третьего цвета.

### 2b. Жест виден, а не описан

Сейчас единственное объяснение — строка `PICK THE CARD. TRUST THE POOL.`

**Сделать три вещи.**

1. **Колода показывает жест сама.** После того как карты улеглись, один раз
   прокатить по вееру **волну**: карты по очереди слева направо коротко
   поднимаются и опускаются на своей дуге, как под невидимым пальцем. Одна
   волна, 0.8–1.0 с, потом покой. Если игрок ничего не трогает 5 с — волна
   повторяется.
2. **Hover на десктопе.** Сейчас `deck.js` слушает только `press*`, поэтому
   мышью выбираешь вслепую: подсветка появляется только после нажатия.
   Подписаться на `hover` / `hoverend` и подсвечивать карту под курсором
   так же, как под пальцем.
3. **Текст.** Подпись остаётся, но получает вторую строку, объясняющую
   руку, а не судьбу: `PICK THE CARD. TRUST THE POOL.` / `DRAG ACROSS THE
   DECK.` Вторая строка — тем же кеглем, приглушённее, и **исчезает
   навсегда после первого касания веера** (в этой сессии).

**Приёмка.** Человек, который видит экран впервые и ничего не читает, ведёт
пальцем по колоде. На десктопе карта под курсором подсвечивается до
нажатия.

---

## 3. Экран 3 — знак тапа на карте и живой переворот

**Файлы:** `src/screens/draw.js`, `src/core/cardRender.js`,
`src/core/gestureGlyph.js`.

### 3a. Фон карты

`drawCardBack` рисует PNG **без подложки** — у рубашки прозрачный фон,
поэтому на экране 3 сквозь неё видно сцену. Добавить ту же заливку
`#000000`, что и в задаче 2a. Один и тот же вид карты во всех сценах.

### 3b. Знак тапа

`CLICK TO DRAW` в шапке, а трогать надо карту в середине экрана — знак и
предмет разнесены, и на самой карте нет ни одного признака, что по ней
можно тапнуть.

**Сделать:** пиксельная **звёздочка** в центре рубашки — новый знак в
`gestureGlyph.js`, рисуется кодом, без ассета. Четырёхлучевая искра из
целых арт-пикселей (ячейка 2 экранных px), пульсирует: разгорается и гаснет
за ~1.4 с, акцентным `#EBA331`. Появляется через 0.6 с после того, как
карта встала на место, живёт до тапа.

Текст `CLICK TO DRAW` **остаётся** — он объясняет, звёздочка показывает
куда.

### 3c. Переворот

Сейчас флип — `scaleX = |cos(progress·π)|` за 0.8 с, линейно по времени.
Выглядит механически: постоянная скорость и жёсткая смена стороны ровно на
середине.

**Сделать:**

- Длительность 0.8 → **1.1 с**.
- Прогресс через easing: разгон и торможение (`easeInOutQuad` уже есть в
  `deck.js` — вынести в общий модуль, не копировать).
- В момент, когда карта стоит ребром (`scaleX` близок к нулю), — короткая
  вспышка белой линией по вертикали: кромка ловит свет.
- Звёздочка гаснет с началом переворота, не резко — за 0.15 с.

**Приёмка.** По карте хочется тапнуть до того, как прочитал подпись.
Переворот смотрится как жест, а не как переключение кадра.

---

## 4. Мини-игра — разная высота плит и облака

**Файлы:** `src/minigames/fool/platforms.js`, `abyss.js`.

Кадр пустой: всё живое умещается в полосу 35–47% высоты, дорога занимает
0,71% кадра (замер 30.08).

**Что сделать.**

1. **Разброс по высоте.** Ступени сейчас всегда ±32. Разрешить ±32 и ±64,
   чтобы маршрут читался как рельеф, а не как линейка. Кратность 32
   экранным px сохраняется (`CLAUDE.md`).
2. **Общее направление — вниз.** Решение 2026-08-11, подтверждено 30.08:
   дорога идёт вправо **и вниз**. Разброс высот не отменяет уклон: сумма
   ступеней отрицательная (в мир, вниз), подъёмы — исключение, не правило.
   Сейчас в `platforms.js` наоборот: шесть подъёмов и один спуск. Это же
   правит расхождение A1 из `docs/open-questions.md`.
3. **Следующая плита в кадре.** Хотя бы одна плита маршрута должна быть
   видна в верхней трети экрана — тогда пустота между ними читается как
   маршрут. Ориентир — макет `docs/interfaces/Mini-game.png`.
4. **Облака — ПОД дорогой, в пропасти,** не над ней. Облака сверху говорят
   «небо»; облака внизу, медленно плывущие, говорят «ты очень высоко», а
   это то, что нужно игре, где можно упасть. Пиксельные, из графического
   языка проекта, два-три слоя с разной скоростью (параллакс), очень
   низкий контраст — они дают глубину, а не рисунок.

`РЕШИТЬ:` число тактов дороги. Консилиум №1 рекомендовал сократить восемь
до трёх. Пользователь хочет платформер с риском, что скорее за то, чтобы
тактов осталось больше. До ответа — количество не трогать, править только
высоты и уклон.

**Приёмка.** На 390×844 в кадре одновременно: текущая плита, кусок
следующей выше или ниже, и облака в пропасти. Полоса «всего живого»
занимает заметно больше 12% высоты кадра.

---

## 5. Мини-игра — падение медленнее и с перетеканием миров

**Файл:** `src/minigames/fool/leap.js` (такт `fall`), `abyss.js`.

Пользователь: «сейчас оно очень быстрое, надо медленное плавное, из мира в
мир перетекающее».

Сейчас: `BRACE_DUR` 0.15 + `FALL_DUR` 2.8 + `ARRIVE_DUR` 1.4, скорость идёт
по дуге с пиком `FALL_VPEAK` 820 px/с. Ощущение скорости даёт именно пик, а
не длительность.

**Что сделать.**

- Пик скорости вниз: `FALL_VPEAK` **820 → 480**. Это главное — падение
  перестаёт быть рывком.
- Слои пропасти при падении меняются **не одинаково**: по мере спуска
  сменяются текстуры (дизер → облака → другая плотность), чтобы читалось
  «мир перетёк в другой мир», а не «фон прокрутился».
- `FALL_V0` и характер дуги оставить: мягкий вход и мягкое оседание уже
  верные.

`РЕШИТЬ — развилка, без ответа не делать длительность:` длинное красивое
падение и частые падения несовместимы. Три варианта:

- **(а)** длинное падение всегда — тогда падать надо редко, а это короткая
  дорога и мягкие края;
- **(б)** короткое падение (0.6–0.8 с) при промахе, длинное перетекание —
  только у финального шага; так главный кадр игры остаётся единственным;
- **(в)** первое падение в карте — длинное (оно новое, это подарок),
  дальше короткое.

Рекомендация — **(в)**, она даёт и то и другое. Но это выбор пользователя.

**Приёмка.** Падение читается как полёт, а не как срыв. Ни в одном кадре
скорость не выглядит рывком.

---

## 6. Аудит размеров: 1 арт-пиксель = 2 или 3 экранных

**Файлы:** `ASSETS.md`, `CLAUDE.md`, `leap.js`, `platforms.js`,
`physics.js`.

Пользователь: «не верю, что персонаж такой маленький, надо перечекать все
размеры».

Факт: Шут — 44×48 арт-пикселей, рисуется ×2, то есть 88×96 экранных. На
стейдже 480×989 это меньше 10% высоты кадра. Он действительно мал.

**Что сделать.**

1. **Сначала аудит, потом код.** Свести в `ASSETS.md` таблицу: файл,
   размер в арт-пикселях, текущий множитель, результат в экранных px, доля
   высоты стейджа на 320×568 / 390×844 / 430×932.
2. **Проверить гипотезу ×3** на ветке: множитель 2 → 3 для всего
   арт-пиксельного слоя (Шут, пёс, тайлы дороги). Множитель целый, сетка не
   плывёт — правило `CLAUDE.md` не нарушается, меняется только число.
3. **Пересчитать всё производное:** высота плиты, ширины плит, щели,
   `PHYS.walk`, скорости прыжка, `DOG_LAG`, `RESPAWN_DROP`, отступы
   подсказок. Иначе дорога станет короче относительно фигуры и вся физика
   поедет.
4. Карта (224×384) и оракул (430×678) — **экранные** пиксели, множитель
   ×1, их это не касается.

`РЕШИТЬ:` ×3 меняет число, записанное в `CLAUDE.md` как правило проекта.
Пробовать на ветке, показать скриншоты на трёх вьюпортах, решение
принимает пользователь. Не мержить без ответа.

**Приёмка.** Скриншоты «до/после» на 320×568, 390×844, 430×932. Дорога и
Шут помещаются в кадр на самом узком; ни один спрайт не масштабируется
дробно; физика ощущается так же, а не «Шут стал тяжелее».
